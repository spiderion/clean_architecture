name: clean_architecture_components
description: Flutter clean architure base components.
version: 0.0.1
homepage: https://github.com/spiderion/clean_architecture

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
